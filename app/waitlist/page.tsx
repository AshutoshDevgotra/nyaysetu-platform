import Waitlist from '../components/Waitlist';

export const metadata = {
    title: 'Join Waitlist | NyayaDwaar',
    description: 'Join the NyayaDwaar waitlist for exclusive access.',
};

export default function WaitlistPage() {
    return <Waitlist />;
}
